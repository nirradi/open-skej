Finish Task 6.10 from `ops/plans/stream-6-plan.md` in this worktree (`/Users/nir.radian/nirdev/skej-stream6`, branch `stream-6/task-6.10`). Read that task's bullet in the plan first.

**A previous run already committed part of it** (commit `134893a`): the `Space` ORM model no longer declares the seven schedule columns, the contract-half migration `2b3a230ee9e1_drop_space_schedule_columns_shim_retired.py` exists, and the architecture docs (`CLAUDE.md`, `.claude/rules/*.md`) are already updated. **Do not touch `.claude/rules/*.md` or `CLAUDE.md` — they are done and are the lead's responsibility.** Do not re-do the model or the migration. The backend is currently in a broken intermediate state because the schemas, the shim, the seed and the frontend still reference columns that no longer exist. Your job is the rest.

## What remains

### Backend

1. `app/backend/app/identity/schemas.py`
   - `SpaceUpdate`: drop `opens_at`, `closes_at`, `slot_minutes`, `max_duration_minutes`, `booking_horizon_days`, `max_bookings_per_week`, `max_bookings_per_month`. It keeps `name`, `description`, `timezone` and both existing validators. Rewrite the docstring so it stops describing the seven fields.
   - `SpaceRead`: drop the same seven fields. `build()` loses its `schedule` argument entirely and goes back to `build(space, role)`. Rewrite its docstring.
   - Delete any `_..._MAX` module constants that only those seven fields used. Keep anything still referenced.
   - `DaySchedule` (~line 635) and anything serving `GET /spaces/{public_id}/schedule` is task 6.9's and **stays exactly as it is** — its `opens_at`/`closes_at`/`slot_minutes` are resolved rule values, not Space columns.

2. `app/backend/app/identity/service.py`
   - Delete the write-through shim: `_unscoped_rows_by_type`, `_write_through_unscoped_rule`, `space_schedule_fields`, and the `AmbiguousRuleInstanceError` class (nothing else raises or catches it).
   - `update_space` becomes what its name says: it applies `name`, `description`, `timezone` only, still honouring the omitted-vs-explicit-null convention via `model_fields_set`, still calling `_require_active`. It no longer touches `space_rules` at all and no longer raises `InvalidOperatingHoursError`.
   - **`InvalidOperatingHoursError` itself stays** — the rules API (`create_space_rule` / `update_space_rule`) raises it and `router.py` catches it at two more places. Only its use from `update_space` goes. Check its docstring still reads correctly once `update_space` is not a caller.
   - `create_space` keeps seeding its default `availability_hours` and `slot_alignment` rows unchanged.

3. `app/backend/app/identity/router.py`
   - Every `SpaceRead.build(...)` call drops its `service.space_schedule_fields(...)` argument (5 call sites).
   - The `update_space` route drops its `except service.InvalidOperatingHoursError` and `except service.AmbiguousRuleInstanceError` handlers, and any comment that describes the write-through behaviour. The other two `InvalidOperatingHoursError` handlers (the rules routes, ~698 and ~724) stay.

4. `app/backend/app/sandbox_seed.py` — the important one.
   - It currently configures Space A and Space B through `service.update_space(SpaceUpdate(...))`, which no longer exists as a path. Replace with direct `SpaceRule` row creation (import `SpaceRule` from `app.identity.models`, or add a small local helper — your call, keep it readable).
   - **Preserve both Spaces' canons exactly as they are today.** Space A: **no** `availability_hours` row at all — `create_space` seeds one by default, so you must delete/not-keep it — plus `slot_alignment` (`SPACE_A_SLOT_MINUTES`), `max_duration` (`SPACE_A_MAX_DURATION_MINUTES`) and `booking_horizon` (`SPACE_A_BOOKING_HORIZON_DAYS`). Space A having no hours is a deliberate fixture property so `app/e2e/tests/03-sad-path.spec.ts` can assert a duration denial with nothing else able to refuse first — the module docstring says so; keep saying so.
   - Space B: `availability_hours` (`SPACE_B_OPENS_AT`/`SPACE_B_CLOSES_AT`), `slot_alignment` (`SPACE_B_SLOT_MINUTES`), `max_duration` (`SPACE_B_MAX_DURATION_MINUTES`), `max_bookings_per_week` (`SPACE_B_MAX_BOOKINGS_PER_WEEK`).
   - Get the exact `rule_type` ids and `params` key names from `rules/rules/registry.py` — the params are the registered schema's own names, not the old column names, and `availability_hours` stores `time.isoformat()` strings. `service._write_through_unscoped_rule`'s current mapping (before you delete it) and the 6.6 backfill migration `22e7e6ba10b3` are both correct references for the shape.
   - Update the module docstring where it describes configuration going through the shim.

5. Backend tests — update every one that reads or writes those seven fields on a Space. At minimum `tests/test_spaces_api.py`, `tests/test_sandbox_seed.py`, `tests/test_rules_stub.py`, `tests/test_resource_bookings_api.py`, `tests/test_operating_hours.py`, `tests/test_rules_api.py`, `tests/test_space_schedule_api.py` — but grep, do not trust this list. Tests that assert the seven fields are on `SpaceRead`, or that `PATCH /spaces` writes them, are testing a contract that is deliberately gone: delete them rather than contorting them. Add a test asserting `SpaceRead` no longer carries the seven fields, so a client cannot read Space configuration by any path but the rules API.

### Frontend (`app/frontend`)

6. `src/api/types.ts` — `Space` loses the seven fields. `DaySchedule`/the `/schedule` types (6.9's) stay.
7. `src/config.ts` — delete `buildCalendarConfig` and the inline param type it reads. It has **no production callers left** (6.9 replaced it with `buildWeekSchedule` / `calendarConfigForDay`); verify that with grep before deleting. Keep `calendarConfig`, `slotsPerDayFor`, `slotStartMinutes`, `slotStart`, `formatSlotLabel`, `isSlotOutOfHours`, `parseClockMinutes`, `uniformWeekSchedule`, `buildWeekSchedule`, `calendarConfigForDay`, `finestSlotMinutes` and anything else still used. Fix the module docstring, which opens by describing `buildCalendarConfig`.
8. `src/config.test.ts` — delete the `buildCalendarConfig` tests, keep the rest.
9. `src/admin/fixtures.ts` and every `*.test.tsx` building a `Space` object with those fields — drop the fields.
10. `src/api/client.ts` — one comment near line 905 references `buildCalendarConfig`; make it true.
11. Check `src/admin/SpaceSchedulePanel*` — 6.8 already reduced it to timezone-only, but confirm it does not read a dropped field.

### E2E (`app/e2e`)

12. Grep for the seven names. `09-space-rules.spec.ts` most likely refers to rule params (fine). Anything asserting Space columns on an API response goes.

## Constraints and acceptance

- **CI gates on formatting.** Run, and make green, from `/Users/nir.radian/nirdev/skej-stream6`:
  - `app/backend`: `black --check .` and `flake8 .` (use the backend's venv if one exists; check `app/backend` for it).
  - `app/backend`: `pytest`. **Three `test_migrations.py` tests fail on an already-migrated developer database** (`DEFERRED.md` item 15) — that is a known standing symptom, not your regression. Everything else must pass. Say in your report exactly which tests failed and why you believe each is item 15.
  - `app/frontend`: `npm run lint` and `npx vitest run` (**foreground, never backgrounded**; the suite is seconds, minutes means hung).
- Do **not** run the Playwright E2E suite locally — it needs clean ports 8000/5173. CI is authoritative for it.
- Keep the repo's prose style in docstrings and comments: present indicative, state the reason, no task numbers where the code is now the description. Where an existing comment says "task 6.6" about the shim you are deleting, the replacement comment should describe the present, not the history.
- Commit on the current branch (`stream-6/task-6.10`) as one or more commits on top of `134893a`, end commit messages with `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`, push, and **open a PR to `main`** titled `Task 6.10: drop the schedule columns and retire the write-through shim`. The PR body should state what changed, what the seed now creates for each Space, and the test results including the item-15 failures.
- Do not edit `ops/`, and do not commit `.task610-prompt.md` — the lead owns both.

Report at the end: the PR URL, the test/lint results verbatim enough to trust, and anything you decided that a reviewer should look at twice.
