annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.14.2
black==26.5.1
certifi==2026.6.17
click==8.4.2
fastapi==0.139.2
flake8==7.3.0
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
idna==3.18
iniconfig==2.3.0
mccabe==0.7.0
mypy_extensions==1.1.0
packaging==26.2
pathspec==1.1.1
platformdirs==4.10.1
pluggy==1.6.0
pycodestyle==2.14.0
pydantic==2.13.4
pydantic_core==2.46.4
pyflakes==3.4.0
Pygments==2.20.0
pytest==9.1.1
python-dotenv==1.2.2
pytokens==0.4.1
PyYAML==6.0.3
SQLAlchemy==2.0.51
starlette==1.3.1
typing-inspection==0.4.2
typing_extensions==4.16.0
uvicorn==0.51.0
uvloop==0.22.1
watchfiles==1.2.0
websockets==16.1.1

# Stream 2 (task 2.1) — Postgres driver, migrations, settings.
# Appended as a block rather than merged alphabetically so Stream 1 never sees a
# conflict on a line it owns. Transitives are pinned too, matching the freeze
# style of the block above.
alembic==1.18.5
Mako==1.3.12
MarkupSafe==3.0.3
psycopg[binary]==3.3.4
psycopg-binary==3.3.4
pydantic-settings==2.14.2

# Stream 2 (task 2.3) — Auth0 JWT verification.
# Appended as a block, matching the 2.1 block above, so Stream 1 never sees a
# conflict on a line it owns. `cryptography` is the extra that PyJWT needs for
# RS256; without it PyJWT silently supports HMAC only.
PyJWT==2.13.0
cryptography==49.0.0
cffi==2.1.0
pycparser==3.0
